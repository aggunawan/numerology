<button
    type="button"
    data-modal-toggle="{{ $modalName ?? null }}"
    class="w-full bg-transparent text-center flex items-center text-base font-bold rounded-lg text-red-500">
    <small class="flex-1 whitespace-nowrap">{{ $trait }}</small>
</button>
